``` mermaid
C4Context
title Museum Companion App - System Context

Person(visitor, "Visitor", "Museum visitor using the mobile companion app")
Person(staff, "Museum Staff", "Museum staff maintaining ticketing / artwork data")
Person(admin, "Museum Admin", "Curates tours, collections, and museum content")

System(museumApp, "Museum Companion App", "Mobile app for museum discovery, artwork viewing, tours, QR access, map, and profile features")
System_Ext(ticketSystem, "Museum Ticketing System", "Provides ticket codes / identifiers")
System_Ext(contentSource, "Museum Content Source", "Artwork metadata, tours, collections, artist information")
System_Ext(mapSource, "Museum Map Source", "Building, floor, and gallery layout information")
System_Ext(mediaSource, "Media / Audio Source", "Artwork images and audio guide content")

Rel(visitor, museumApp, "Uses", "iOS app")
Rel(staff, ticketSystem, "Maintains ticket / entry data")
Rel(admin, contentSource, "Curates content")
Rel(admin, mapSource, "Updates map / gallery layout")

Rel(museumApp, ticketSystem, "Validates ticket code / unlocks access", "API / local integration")
Rel(museumApp, contentSource, "Reads artworks, artists, collections, tours", "API / mock / local store")
Rel(museumApp, mapSource, "Reads museum map data", "API / local store")
Rel(museumApp, mediaSource, "Loads artwork images and audio", "CDN / local assets")
```